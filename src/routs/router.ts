export default function Router(){

    const pathname:string = window.location.pathname

    switch(pathname){
        case '/':
            break;
        case "/about":
            break;
        case "/users":
            break
        default:
    }
}